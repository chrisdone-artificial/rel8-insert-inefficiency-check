# with Rel8.prepared

```sql
Connected to database
encodeUtf8 ... => ditto
Text.pack sql => ditto
(show doc) => INSERT INTO "files" ("content")
VALUES
(CAST($1 AS "bytea"))
File inserted successfully!
```
